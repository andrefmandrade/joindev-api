{
  "name": "src",
  "version": "1.0.0",
  "main": "api/src/index.js",
  "license": "MIT",
  "scripts": {
    "deploy": "cd api && npx yarn && npx knex migrate:latest && node src/index.js"
  }
}
